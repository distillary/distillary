Write a personal reaction to a claim or concept.

What to annotate: $ARGUMENTS

**Examples:**

```
/dist:annotate the integrity argument
/dist:annotate Rich Dad's view on debt vs Ibn Qayyim's view on riba
/dist:annotate I disagree with the claim that fear drives all financial decisions
```

Read `.claude/agents/annotate.md` FIRST.

**Asks you:** What's your reaction? Agreement, disagreement, question, connection to something else?

Then creates a properly formatted note in `brain/personal/annotations/` with:
- [[wikilinks]] to the specific claims and entities you're reacting to
- Your reaction structured as a first-class note in the graph
- Tags linking it to the source and domain
